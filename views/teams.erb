<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= params[:team] %></title>
  </head>

  <body>
    <h1><%= params[:team]%></h1>
    <table style="width:300px;text-align:left">
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Position</th>
    </tr>
    <% @rosters.each do |roster| %>
      <% if roster[:team] == params[:team] %>
        <tr>
          <td><%= "#{roster[:first_name]}" %></td>
          <td><%= "#{roster[:last_name]}" %></td>
          <td><%= "#{roster[:position]}" %></td>
        </tr>
      <% end %>
    <% end %>
    </table>
    <a href="/">--Back--</a>
  </body>
</html>
